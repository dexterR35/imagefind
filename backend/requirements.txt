fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
numpy==1.26.4
pillow==12.3.0
open-clip-torch==2.26.1
# Pulls in opencv-python-headless as its own transitive dependency — no need
# to pin that separately, and app/ never imports cv2 directly.
easyocr==1.7.2
watchdog==6.0.0
# Used directly by model_download.py to stream the RAM++ checkpoint from
# Hugging Face with progress reporting - already pulled in transitively by
# huggingface-hub, but pinned explicitly since app/ now imports it directly.
requests==2.34.2
# Password hashing for the local shared account. The argon2 extra installs
# the maintained backend used by PasswordHash.recommended().
pwdlib[argon2]==0.3.0
# SQLite-native KNN vector search. Embeddings stay in index.db and are queried
# through a vec0 virtual table instead of being loaded into a process-wide
# NumPy matrix at startup.
sqlite-vec==0.1.9
# fairscale and transformers aren't imported directly anywhere in app/ — they're
# used internally by the ram package (ram/models/bert.py, vit.py) and aren't
# declared in its own package metadata, so they're pinned explicitly here.
fairscale==0.4.13
transformers==5.15.1
# Vendored locally in vendor/recognize-anything (pruned copy of
# https://github.com/xinyu1205/recognize-anything, training-only code and
# demo assets stripped) — not on PyPI, and this way `pip install -r
# requirements.txt` doesn't need git or network access to build it.
./vendor/recognize-anything
pytest==9.1.1
httpx==0.28.1
