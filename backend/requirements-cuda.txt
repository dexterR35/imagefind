# CUDA 12.6 is intentional: it is the newest PyTorch wheel line that
# supports Pascal GPUs such as the GTX 1080 Ti (compute capability 6.1).
# The CUDA-enabled wheel still falls back to CPU when no GPU is available.
--index-url https://download.pytorch.org/whl/cu126
torch==2.10.0
torchvision==0.25.0
